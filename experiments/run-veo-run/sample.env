# Google Cloud Project Configuration
# Set your project ID here
GOOGLE_CLOUD_PROJECT=your-project-id
GOOGLE_CLOUD_LOCATION=us-central1

# Service Account (Created via ./setup-sa.sh)
SERVICE_ACCOUNT_EMAIL=sa-run-veo-run@your-project-id.iam.gserviceaccount.com

# Model Configuration
GEMINI_MODEL=gemini-3-flash-preview
GEMINI_MODEL_LOCATION=global # optional, will use GOOGLE_CLOUD_LOCATION if not present
VEO_MODEL=veo-3.1-fast-generate-preview

# Storage Configuration
# Bucket for storing generated video assets (must be writable by the SA)
VEO_BUCKET=your-asset-bucket-name

# Server Configuration
PORT=8080

# Security
# Max requests per minute per IP. Global quota is ~10 RPM, so keep this low (e.g. 3-5).
RATE_LIMIT_PER_MINUTE=3